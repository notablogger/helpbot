---
description: Use notablogger GitHub identity for all git and gh operations
alwaysApply: true
---

# GitHub Identity

This project uses the **notablogger** GitHub account exclusively.

- Git author: `notablogger <69604730+notablogger@users.noreply.github.com>` (local repo config)
- Remote: `git@github.com:notablogger/helpbot.git`
- `gh` CLI: authenticated as `notablogger`

Do not use the global git identity (`nikhil goyal` / Mercedes email) for commits or PRs in this repo.
